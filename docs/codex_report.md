## 今回やったこと
- study-app の出題開始処理が、選択された出題数で実際の `state.questions` を slice していることをテストで固定しました。
- RPG 側にもランダム出題 ON/OFF を追加し、OFF では元の順番、ON では全問題をシャッフルしてから選択件数だけ出題するようにしました。
- RPG のバトル表示に実際の出題数を使った進捗（例: `1 / 10`）を表示するようにしました。
- 出題数制限の回帰テストを追加し、npm test に組み込みました。

## 変更ファイル
- `study-app/script.js`: 出題数制限ロジックのテスト対象として確認（実装は既に slice 済み）。
- `script.js`: RPG 側の出題デッキ生成を選択件数・ランダム設定に対応。
- `index.html`: RPG 側にランダム出題チェックボックスを追加。
- `tests_question_count.js`: study-app/RPG 両方の出題数制限テストを追加。
- `package.json`: 追加テストを `npm test` に組み込み。
- `docs/codex_report.md`: 今回の作業内容を記録。

## テスト結果
- `npm test`: PASS。既存テストと追加した出題数制限テストが成功しました。
- `node /tmp/screenshot.js`: FAIL。Playwright が依存関係に無く、スクリーンショット取得はできませんでした。

## 注意点
- RPG 側はこれまで常にランダム出題でしたが、今回からチェックボックス OFF の初期状態では元の順番で出題します。
- study-app 側の実装は既に `orderedPool.slice(0, requestedCount)` を使っていたため、主に回帰テストで仕様を固定しました。
- UI 変更があるためスクリーンショット取得を試みましたが、実行環境に Playwright がありませんでした。

## 次にやるべきこと
- ブラウザで study-app と RPG を開き、10問/20問/全問、ランダム ON/OFF の手動確認を行う。
- 必要なら Playwright などのブラウザテスト環境を導入し、進捗表示の E2E テストを追加する。

## チャッピーに相談すべき点
- RPG 側のランダム出題チェックボックスを初期 ON にして従来挙動を優先するか、今回の仕様どおり初期 OFF の元の順番を優先するか。
