## 今回やったこと
- study-app の勉強数カウンターで、累計の合計が既存 `total` に引っ張られず、表示中のモード別内訳（英単語 / チャンク / 文節 / 英文）の合算になるよう修正しました。
- 今日・今月・今年・累計の全期間で、`word + chunk + phrase + definition = total` が成立することをテストで確認しました。
- version 1 由来の `total` / `byDate` は読み込み・保存上保持しつつ、結果画面のモード別合計表示には使わない仕様に整理しました。
- README / docs/project_status.md の説明から「累計合計のみ既存 total を尊重する」旨を削除し、現在仕様へ更新しました。

## 変更ファイル
- `study-app/script.js`
- `tests_study_app_study_counts.js`
- `README.md`
- `docs/project_status.md`
- `docs/codex_report.md`

## テスト結果
- `npm test` を実行し、全テストが成功しました。
- 実行時に npm の `Unknown env config "http-proxy"` 警告が表示されましたが、テスト自体は成功しています。

## 注意点
- 既存 version 1 データの `total` / `byDate` は localStorage 上の互換データとして保持しますが、結果画面の「累計の勉強数」の合計には使いません。
- version 1 由来でモード別内訳がない過去分は、これまでどおり各モードへ自動分配しません。そのため、表示上の累計合計は `byMode` に記録済みの内訳合計です。
- UIの見た目自体は変更していないため、スクリーンショットは取得していません。

## 次にやるべきこと
- 実機ブラウザで、既存データを持つ端末の結果画面にて「英単語 + チャンク + 文節 + 英文 = 合計」になっているか確認するとより安心です。

## チャッピーに相談すべき点
- version 1 由来の内訳不明な過去分を将来的に別行（例: 内訳不明）で見せるべきかどうか。
